services:
  metube:
    image: ghcr.io/alexta69/metube:latest
    container_name: metube
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
    volumes:
      - metube-downloads:/downloads
    labels:
      tsdproxy.enable: "true"
      tsdproxy.name: "metube"
      tsdproxy.port.1: "443/https:8081/http"
    restart: always

volumes:
  metube-downloads:
