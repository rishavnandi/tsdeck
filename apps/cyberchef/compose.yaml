services:
  cyberchef:
    image: ghcr.io/gchq/cyberchef:latest
    container_name: cyberchef
    environment:
      - TZ=${TZ}
    labels:
      tsdproxy.enable: "true"
      tsdproxy.name: "cyberchef"
      tsdproxy.port.1: "443/https:8000/http"
    restart: always
