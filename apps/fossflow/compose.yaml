services:
  fossflow:
    image: stnsmith/fossflow:latest
    container_name: fossflow
    environment:
      - TZ=${TZ}
      - PUBLIC_URL=${PUBLIC_URL}
    labels:
      tsdproxy.enable: "true"
      tsdproxy.name: "fossflow"
      tsdproxy.port.1: "443/https:80/http"
    restart: always
